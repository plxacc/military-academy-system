<!-- views/partials/loader.ejs -->
<div id="globalLoader" class="fixed inset-0 z-[99999] flex flex-col items-center justify-center bg-[#05070a]/85 backdrop-blur-xl transition-opacity duration-300 opacity-0 pointer-events-none">
    <div class="relative flex flex-col items-center scale-90 sm:scale-100">
        <!-- درع الكلية مع تأثير النبض وحلقة الدوران -->
        <div class="relative w-24 h-24 rounded-3xl bg-gradient-to-br from-orange-600 to-amber-700 flex items-center justify-center shadow-[0_0_50px_rgba(249,115,22,0.4)] border border-orange-400/50 mb-8 z-10">
            <span class="text-5xl animate-pulse">🛡️</span>
            <!-- حلقة تدور حول الدرع -->
            <div class="absolute inset-0 rounded-3xl border-4 border-transparent border-t-white/60 border-b-white/60 animate-[spin_1.5s_linear_infinite]"></div>
        </div>
        
        <h3 class="text-white font-black tracking-widest text-2xl mb-3">الكلية العسكرية</h3>
        <div class="flex items-center gap-3 bg-[#0e1218]/90 px-6 py-3 rounded-full border border-[#1f2630] shadow-inner">
            <div class="w-5 h-5 border-2 border-orange-500 border-t-transparent rounded-full animate-spin"></div>
            <p class="text-orange-400 font-mono text-sm font-bold tracking-wide">جاري سحب البيانات الآمنة...</p>
        </div>
    </div>
</div>

<script>
    // سكربت ذكي يعترض الضغطات ويظهر التحميل فوراً
    document.addEventListener('DOMContentLoaded', () => {
        const loader = document.getElementById('globalLoader');
        
        function showLoader() {
            loader.classList.remove('opacity-0', 'pointer-events-none');
            loader.classList.add('opacity-100', 'pointer-events-auto');
        }

        function hideLoader() {
            loader.classList.remove('opacity-100', 'pointer-events-auto');
            loader.classList.add('opacity-0', 'pointer-events-none');
        }

        // إخفاء التحميل أول ما تفتح الصفحة
        hideLoader();

        // إخفاء التحميل في حال ضغط زر "الرجوع" في المتصفح لتفادي التعليق
        window.addEventListener('pageshow', (e) => {
            if (e.persisted) hideLoader();
        });

        // مراقبة جميع الروابط في الموقع
        document.querySelectorAll('a').forEach(link => {
            link.addEventListener('click', function(e) {
                const href = this.getAttribute('href');
                const target = this.getAttribute('target');
                
                // التفعيل فقط للروابط الداخلية اللي تنقل لصفحة ثانية وتتطلب سحب بيانات
                if (href && href.startsWith('/') && !href.startsWith('#') && target !== '_blank' && !href.includes('logout')) {
                    showLoader();
                }
            });
        });

        // إظهار اللودر عند إرسال أي فورم للقيادة
        document.querySelectorAll('form').forEach(form => {
            form.addEventListener('submit', () => showLoader());
        });
    });
</script>